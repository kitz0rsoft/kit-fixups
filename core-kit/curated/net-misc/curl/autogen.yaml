curl:
  generator: github-1
  packages:
    - curl:
        version: '8.12.0'
        github:
          query: releases
          tarball: curl-{version}.tar.gz
          match: curl-(.*)
          transform:
            - kind: string
              match: _
              replace: .

